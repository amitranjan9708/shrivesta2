# Frontend Environment Variables

# Environment: 'local' or 'production'
# When set to 'production', uses VITE_RENDER_API_URL
# When set to 'local' (or omitted), uses localhost
VITE_ENV=production

# Production Render API URL (used when VITE_ENV=production)
VITE_RENDER_API_URL=https://your-backend-service.onrender.com/api/v1

# Optional: Override API URL directly (not recommended when using VITE_ENV)
# VITE_API_BASE_URL=http://localhost:3000/api/v1

# Stripe Configuration
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
